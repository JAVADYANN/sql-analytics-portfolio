# sql-analytics-portfolio

-Create a repository on GitHub.
-Connect VS Code with the repository.
-Create a folder named `data` and add our data there.
-Create a `.env` file.
-After that, create a `docker-compose` file.
-Go to the local host.
-Write a SELECT query, and all our data will be displayed below.
